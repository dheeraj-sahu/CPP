#include <bits/stdc++.h>
using namespace std;
void explainUSet(){
    //unsorted
    //unique i.e. no duplicates
    unordered_set<int> st;
    /*
    all operations are the same as set except lower_bound and upper_bound
    and time complexity of all of these is O(1)
    time complexity goes O(n) in worst case --> Once is blue moon
    */

}